{% extends 'base.html.twig' %}

{% block title %}
Me contacter - {{ parent() }}
{% endblock title %}

{% block body %}

    {% for message in app.flashes('success') %}
        <div class="alert alert-success" role="alert">{{ message }}</div>
    {% endfor %}

<div class="inner_body bg_gray">
    <!-- Page Header -->
    <div class="page-header type2">
        <div class="page-header-content text-center">
            <div class="container">
                <h1 class="heading">
                    Me contacter
                    <span class="icon"><i class="fa fa-envelope"></i></span>
                </h1>
            </div>
        </div>
    </div>
    <!-- End Page Header -->

    <div class="contact_body">
        <div class="container">
            <div class="row">
                <div class="col-lg-6">
                    <div class="contact_info">
                        <h3 class="title">Mes coordonnées</h3>
                        <p class="short_desc">Pour plus d'informations sur mes services et tarifs, n'hésitez pas à me contacter via les coordonnées ci-dessous où en m'envoyant en mail à l'aide du formulaire.</p>

                        <h4>Numéro de téléphone :</h4>
                        <p>06 30 11 29 17</p>
                        <h4>Email :</h4>
                        <p>contact@remi-delecroix.com</p>
                    </div>
                </div>
                <div class="col-lg-6">
                    <div class="contact_form">
                        <h3 class="title">Formulaire de contact</h3>
                        {{ form_start(form) }}
                            <div class="row">
                                <div class="col-lg-6">
                                    <div class="input_field">
                                        {{ form_widget(form.prenom, { 'attr': { 'class': 'form-control' } }) }}
                                        {{ form_errors(form.prenom) }}
                                        {{ form_label(form.prenom, '', { 'label_attr': { 'for': 'prenom' } }) }}
                                        <span class="required-sm">obligatoire</span>
                                    </div>
                                </div>
                                <div class="col-lg-6">
                                    <div class="input_field">
                                        {{ form_widget(form.nom, { 'attr': { 'class': 'form-control' } }) }}
                                        {{ form_errors(form.nom) }}
                                        {{ form_label(form.nom, '', { 'label_attr': { 'for': 'nom' } }) }}
                                        <span class="optional-sm">facultatif</span>
                                    </div>
                                </div>
                                <div class="col-lg-6">
                                    <div class="input_field">
                                        {{ form_widget(form.email, { 'attr': { 'class': 'form-control' } }) }}
                                        {{ form_errors(form.email) }}
                                        {{ form_label(form.email, '', { 'label_attr': { 'for': 'email' } }) }}
                                        <span class="required-sm">obligatoire</span>
                                    </div>
                                </div>
                                <div class="col-lg-6">
                                    <div class="input_field">
                                        {{ form_widget(form.telephone, { 'attr': { 'class': 'form-control' } }) }}
                                        {{ form_errors(form.telephone) }}
                                        {{ form_label(form.telephone, '', { 'label_attr': { 'for': 'telephone' } }) }}
                                        <span class="optional-sm">facultatif</span>
                                    </div>
                                </div>
                                <div class="col-lg-6">
                                    <div class="input_field">
                                        {{ form_widget(form.objet, { 'attr': { 'class': 'form-control' } }) }}
                                        {{ form_errors(form.objet) }}
                                        {{ form_label(form.objet, '', { 'label_attr': { 'for': 'objet' } }) }}
                                        <span class="required-sm">obligatoire</span>
                                    </div>
                                </div>
                                <div class="col-lg-12">
                                    <div class="input_field">
                                        {{ form_widget(form.message, { 'attr': { 'id': 'contact-message', 'class': 'form-control md-textarea', 'rows': '3' } }) }}
                                        {{ form_errors(form.message) }}
                                        {{ form_label(form.message, '', { 'label_attr': { 'for': 'message' } }) }}
                                        <span class="required-sm">obligatoire</span>
                                    </div>

                                    <div class="button_group">
                                        <input type="submit" class="button black_button" value="Envoyer">
                                    </div>
                                </div>
                            </div>
                        {{ form_end(form) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

    <div class="page_title"><span>Me contacter</span></div>
{% endblock %}

