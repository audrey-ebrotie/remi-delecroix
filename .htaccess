#Notez que l'utilisation de ce fichier suppose que vous ayez le contrôle sur votre configuration Apache 
#et que vous ayez activé l'option AllowOverride pour permettre l'utilisation des fichiers .htaccess.

#Ce fichier .htaccess comprend des directives pour :

    #Activer la réécriture d'URL pour utiliser des URL plus "propres"
    #Activer la compression pour diminuer la taille des fichiers envoyés au navigateur
    #Forcer l'utilisation de HTTPS pour améliorer la sécurité
    #Activer le cache du navigateur pour améliorer les performances
    #Supprimer les ETags pour éviter des problèmes de performance liés au cache

# Commencez par activer la réécriture d'URL pour utiliser des URL "propres" (par exemple, /about au lieu de /index.php?page=about)
RewriteEngine On

# Si la requête n'est pas pour un fichier réel
RewriteCond %{REQUEST_FILENAME} !-f
# Si la requête n'est pas pour un répertoire réel
RewriteCond %{REQUEST_FILENAME} !-d
# Réécrit toutes les requêtes vers le fichier index.php
RewriteRule ^ index.php [QSA,L]

# Activer la compression pour améliorer les performances
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/html
    AddOutputFilterByType DEFLATE text/css
    AddOutputFilterByType DEFLATE text/javascript
    AddOutputFilterByType DEFLATE text/xml
    AddOutputFilterByType DEFLATE text/plain
    AddOutputFilterByType DEFLATE image/x-icon
    AddOutputFilterByType DEFLATE image/svg+xml
    AddOutputFilterByType DEFLATE application/rss+xml
    AddOutputFilterByType DEFLATE application/javascript
    AddOutputFilterByType DEFLATE application/x-javascript
    AddOutputFilterByType DEFLATE application/xml
    AddOutputFilterByType DEFLATE application/xhtml+xml
    AddOutputFilterByType DEFLATE application/x-font
    AddOutputFilterByType DEFLATE application/x-font-truetype
    AddOutputFilterByType DEFLATE application/x-font-ttf
    AddOutputFilterByType DEFLATE application/x-font-otf
    AddOutputFilterByType DEFLATE application/x-font-opentype
    AddOutputFilterByType DEFLATE application/vnd.ms-fontobject
    AddOutputFilterByType DEFLATE font/ttf
    AddOutputFilterByType DEFLATE font/otf
    AddOutputFilterByType DEFLATE font/opentype
</IfModule>

# Forcer l'utilisation de HTTPS pour améliorer la sécurité
<IfModule mod_rewrite.c>
    RewriteCond %{HTTPS} off
    RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
</IfModule>

# Activer le cache du navigateur pour améliorer les performances
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType image/jpg "access 1 year"
    ExpiresByType image/jpeg "access 1 year"
    ExpiresByType image/gif "access 1 year"
    ExpiresByType image/png "access 1 year"
    ExpiresByType text/css "access 1 month"
    ExpiresByType text/html "access 1 month"
    ExpiresByType application/pdf "access 1 month"
    ExpiresByType text/x-javascript "access 1 month"
    ExpiresByType application/x-shockwave-flash "access 1 month"
    ExpiresByType image/x-icon "access 1 year"
    ExpiresDefault "access 1 month"
</IfModule>

# Supprimer les ETags pour améliorer les performances
<IfModule mod_headers.c>
    Header unset ETag
</IfModule>
FileETag None

AddType video/mp4 .mp4